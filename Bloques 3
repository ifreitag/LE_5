#include <stdio.h>

int main(void) {
  int max;
  printf("Ingrese el maximo: ");
  scanf("%d",&max);
  if (max<3) printf("No es valido");
  else{
    for (int u=0;u<max;u++){
      if (u%3==0){
        printf("%d\n",u);
      }
    }
  }
  return 0;
}
