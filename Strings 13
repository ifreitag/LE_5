#include <stdio.h>
#include <string.h>
int main(void) {
  char pal[50];
  char let;
  printf("Ingrese una palabra: ");
  scanf("%s",&pal);
  for (int t=0; t<50; t++){
    if (pal[t] == '\0'){
      let = t;
      t=50;

    }

  }
  printf("tiene %d caracteres",let);
  return 0;
}
